<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.VacancySearch" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/blue</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorSurface</item>

        <!-- Bottom Navigation Styles -->
        <item name="bottomNavigationStyle">@style/Widget.VacancySearch.BottomNavigationView</item>
    </style>

    <!-- Bottom Navigation Widget Style -->
    <style name="Widget.VacancySearch.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/bottom_nav_selected</item>
        <item name="itemTextColor">@color/bottom_nav_selected</item>
        <item name="itemBackground">@android:color/transparent</item>
        <item name="materialThemeOverlay">@style/ThemeOverlay.VacancySearch.BottomNavigationView</item>
    </style>

    <!-- Theme overlay for Bottom Navigation -->
    <style name="ThemeOverlay.VacancySearch.BottomNavigationView" parent="">
        <item name="colorPrimary">@color/bottom_nav_selected</item>
    </style>
</resources>
